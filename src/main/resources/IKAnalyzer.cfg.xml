<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">  
<properties>  
	<comment>IK Analyzer 扩展配置</comment>

	<!-- the main dictionary (Only One) -->
	<entry key="core-main">
		core-main.dic
	</entry>

	<!-- the quantifier dictionary (Only One) -->
	<entry key="core-quantifier">
		core-quantifier.dic
	</entry>

	<!-- the extension dictionary for user (multi-dicts segmented by comma) -->
	<entry key="ext_dict">
		ext.dic;
	</entry> 
	
	<!-- the stop words dictionary for user (multi-dicts segmented by comma) -->
	<entry key="ext_stopwords">
		stopword.dic;
	</entry> 
	
</properties>